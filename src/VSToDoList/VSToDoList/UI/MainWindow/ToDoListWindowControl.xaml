<UserControl x:Class="VSToDoList.UI.MainWindow.ToDoListWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:VSToDoList.Models"
             xmlns:view="clr-namespace:VSToDoList.UI.MainWindow"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             Name="thisWindow"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <UserControl.Resources>
        <view:ToDoListWindowViewModel x:Key="ViewModel"/>
    </UserControl.Resources>

    <Grid DataContext="{StaticResource ViewModel}">
        <TreeView x:Name="myTreeView" Background="{DynamicResource VsBrush.Window}" ItemsSource="{Binding Path=TasksList}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type models:Task}" ItemsSource="{Binding SubTasks}">
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox IsChecked="{Binding IsDone,Mode=TwoWay}" Grid.Column="0"/>
                        <TextBlock Text="{Binding Name}" Foreground="White" Grid.Column="1"/>
                        <TextBlock Text="+" HorizontalAlignment="Right" Grid.Column="2" />
                    </Grid>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>
